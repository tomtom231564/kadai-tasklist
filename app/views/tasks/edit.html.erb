<!--タスク編集ページ-->
<h1>id: <%= @task.id %> のタスク編集ページ</h1>

<%#= form_with(model: @task) do |f| %>

  <%#= f.label :content, 'タスク' %>
  <%#= f.text_field :content %>
  <%#= f.submit '登録' %>
<%# end %>

<%= render 'form', task: @task %>
<%= link_to 'TOPへ戻る',root_path %><br>
<%= link_to 'このタスクを削除する', @task, method: :delete, data: { confirm: '本当に削除してよろしいですか？' } %>